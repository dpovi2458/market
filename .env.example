############################################################
# Archivo de ejemplo de variables de entorno (.env.example) #
# NO usar valores reales aquí. Copia a .env.local y completa #
# Luego rota tus claves si expusiste credenciales antes.      #
############################################################

# Cadena de conexión MongoDB Atlas (reemplaza <user>, <pass>, <cluster>)
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster-id>.mongodb.net/marketplace?retryWrites=true&w=majority

# Secreto para firmar JWT (usa al menos 32-64 chars aleatorios)
JWT_SECRET=REEMPLAZA_CON_UNA_CADENA_SEGURA

# Cloudinary (solo el cloud name es público, las otras claves son sensibles)
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=tu_cloud_name
CLOUDINARY_API_KEY=tu_api_key
CLOUDINARY_API_SECRET=tu_api_secret

# URL pública de la app (en dev dejar http://localhost:3000)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Nombre del upload preset UNSIGNED configurado en Cloudinary
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=marketplace_unsigned

# NOTAS DE SEGURIDAD:
# 1. No subas .env.local al repositorio.
# 2. Si estas claves ya fueron públicas, genera nuevas en MongoDB/Cloudinary.
# 3. Limita formatos y tamaños en el preset Unsigned de Cloudinary.
